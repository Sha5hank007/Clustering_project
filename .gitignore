# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# ── Environment ──
.env

# ── InsightFace models (downloaded at runtime, ~1GB) ──
# These live at ~/.insightface/models/ on host
# Inside Docker they download to /root/.insightface/models/
*.onnx

# ── Stored face crops (generated by pipeline) ──
data/crops/*
!data/crops/.gitkeep

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Docker ──
docker-compose.override.yml

# ── Alembic ──
*.pyc

# ── Frontend ──
frontend/node_modules/
frontend/dist/
frontend/.env
